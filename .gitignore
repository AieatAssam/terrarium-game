node_modules
dist
dist-ssr
*.local
.DS_Store

# Vite / TS build cache
node_modules/.tmp
*.tsbuildinfo

# Playwright
/test-results/
/playwright-report/
/playwright/.cache/
/blob-report/

# Vitest / coverage
coverage

# Playwright trace dumps under QA evidence — transient diagnostic output,
# not durable evidence (screenshots themselves ARE tracked)
docs/qa-screenshots/**/trace.json

# Copyrighted third-party reference screenshots — internal-only, never commit
docs/reference-candidates/
docs/references/**/*.jpg
docs/references/**/*.jpeg
docs/references/**/*.png
docs/references/**/*.webp
